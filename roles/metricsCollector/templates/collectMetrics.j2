#!/bin/bash
RESULT=$( {{ item.command }} )
echo "{{ item.group }},name={{ item.name }},host=$(hostname) value=$RESULT" | curl -s -XPOST 'http://127.0.0.1:{{ metricCollectors.influx.port }}/write?db={{ influx.db.name }}' --data-binary @-
echo $RESULT
